import React from "react";

type Props = {};

const Footer = (props: Props) => {
  return (
    <footer className="absolute bottom-0 w-full bg-black min-h-[100px]">
      <div className="lg:max-w-[1100px] w-[90%]"></div>
    </footer>
  );
};

export default Footer;
